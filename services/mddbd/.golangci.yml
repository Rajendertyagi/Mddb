version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - misspell
    - revive

linters-settings:
  gosec:
    excludes:
      - G104
      - G115

run:
  timeout: 5m

issues:
  max-issues-per-linter: 50
